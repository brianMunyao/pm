import React from 'react';
import styled from 'styled-components';

const ProjectCard = () => {
	return (
		<Container>
			<p className="pc-title">Freedom App</p>
			<p className="pc-type"></p>
		</Container>
	);
};

const Container = styled.div``;

export default ProjectCard;
